import { CodeEditorRef } from '@openshift-console/dynamic-plugin-sdk';
export declare const useEditorHighlighter: (editableYAML: string, pathsToHighlight: string[], showEditor: boolean) => (ref: CodeEditorRef) => void;
