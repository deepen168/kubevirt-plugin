import { IDLabel } from './types';
export declare const nodeSelectorToIDLabels: (nodeSelector: {
    [key: string]: string;
}) => IDLabel[];
export declare const isEqualObject: (object: any, otherObject: any) => boolean;
