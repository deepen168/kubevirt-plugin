import * as React from 'react';
// If translation is needed, sent text as a prop which is already translated
var MutedTextSpan = function (_a) {
    var text = _a.text;
    return React.createElement("span", { className: "text-muted" }, text);
};
export default MutedTextSpan;
//# sourceMappingURL=MutedTextSpan.js.map