import React from 'react';
declare type CompletionTimeoutProps = {
    setState: React.Dispatch<React.SetStateAction<number>>;
    state: number;
};
declare const CompletionTimeout: React.FC<CompletionTimeoutProps>;
export default CompletionTimeout;
