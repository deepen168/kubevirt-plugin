export declare const getBootVolumeOS: (bootVolume: BootableVolume) => OS_NAME_TYPES;
export declare const getPaginationFromVolumeIndex: (volumeIndex: number) => (prevPagination: PaginationState) => PaginationState;
