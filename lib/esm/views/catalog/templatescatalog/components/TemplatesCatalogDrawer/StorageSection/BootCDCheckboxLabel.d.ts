import { FC } from 'react';
declare type BootCDCheckboxProps = {
    hasCDSource: boolean;
    onChange: (checked: boolean) => void;
};
declare const BootCDCheckbox: FC<BootCDCheckboxProps>;
export default BootCDCheckbox;
