import * as React from 'react';
import { V1Template } from '@kubevirt-utils/models';
declare type CreatedAtProps = {
    template: V1Template;
};
declare const CreatedAt: React.FC<CreatedAtProps>;
export default CreatedAt;
