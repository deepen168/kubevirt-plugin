import { SearchItem } from '../../utils/search';
export declare const createConfigurationSearchURL: (tab: string, element: string, pathname: string) => string;
export declare const getSearchItemsIds: (searchItems: SearchItem[]) => string[];
